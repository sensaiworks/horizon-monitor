anthropic>=0.40.0
mcp>=1.0.0
chromadb>=0.5.0
imagehash>=4.3.1
Pillow>=10.0.0
pydantic>=2.0.0
tomli>=2.0.0; python_version < "3.11"   # stdlib `tomllib` only exists on 3.11+; backport for older
plyer>=2.1.0
python-dotenv>=1.0.0

# Embeddings (choose one — see config.toml embedding_provider)
voyageai>=0.2.0          # preferred: voyage-3 via API
sentence-transformers>=3.0.0  # offline fallback

# CLI + tray
click>=8.1.0
pystray>=0.19.0           # legacy `tray` command only

# Desktop UI (`app` command): tray + tabbed window. The cp310-abi3 wheel runs on 3.10–3.14.
PySide6>=6.8.0
